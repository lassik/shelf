*              a=$(($a * $b))
+              a=$(($a + $b))
-              a=$(($a - $b))
<              a=$(($a < $b))
=              a=$(($a = $b))
>              a=$(($a > $b))

list-null?     [[ -z $a ]]

string-null?   [[ -z $a ]] && a=t || a=''
string=?       [[ $a = $b ]] && a=t || a=''
